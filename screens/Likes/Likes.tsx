import React from 'react';
import { View, Text } from 'react-native';
import { Menu } from '../../components/UI/Menu/Menu';

export const Likes = () => {
	return (
		<>
			<View>
				<Menu name={'sasha'} />
			</View>
		</>
	);
};
